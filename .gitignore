.DS_Store
node_modules/
dist/
.env
.env.local
.bun
bun.lockb

# @ua/ua-tokens ships its compiled dist as a snapshot so downstream templates
# can consume it without a build step.
!packages/ua-tokens/dist/
