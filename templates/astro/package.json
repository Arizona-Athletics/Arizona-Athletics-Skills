{
  "name": "@ua/template-astro",
  "version": "0.1.0",
  "description": "University of Arizona Athletics starter — Astro 6 + Tailwind 4 wired to @ua/ua-tokens. PKCE auth (Cognito + UA SAML) is stubbed; see docs/AUTH.md.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check && tsc --noEmit"
  },
  "dependencies": {
    "@ua/ua-tokens": "workspace:*",
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^22.10.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.7.0"
  }
}
